package {{package}}.apis

import io.ktor.client.*
import io.ktor.client.request.*
{{#imports}}
import {{import}}
{{/imports}}
888888888888888
/*
 * {{fileComment}}
 */
class {{classname}}(private val client: HttpClient) {
{{#operations}}
    /**
     * Handles the operation: {{summary}}
     */
    suspend fun {{nickname}}(
        {{#allParams}}
        {{paramName}}: {{dataType}}{{^last}},{{/last}}
        {{/allParams}}
    ): {{#returnType}}{{returnType}}{{/returnType}}{{^returnType}}kotlin.Unit{{/returnType}} {
        return client.get("{{path}}")
    }
{{/operations}}
}